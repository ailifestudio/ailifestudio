name: Auto Update Blog with AI

on:
  schedule:
    - cron: '0 0,6,12 * * *'
  workflow_dispatch:
    inputs:
      manual_topic:
        description: '제목/주제를 입력하세요 (필수)'
        required: true
        type: string
      manual_content:
        description: '본문 내용을 입력하세요 (비워두면 AI가 창작, 채우면 AI가 정리+이미지추가)'
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  update-blog:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
    - name: 📥 체크아웃
      uses: actions/checkout@v4
      
    - name: 🐍 Python 설정
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: 📦 의존성 설치
      run: |
        cd automation
        pip install -r requirements.txt
        
    # [Step 1] 주제 설정 (제목은 필수)
    - name: 🚀 Step 1 - 주제 선정
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        MANUAL_TOPIC: ${{ github.event.inputs.manual_topic }}
      run: |
        mkdir -p automation/intermediate_outputs
        # 제목이 없으면 자동, 있으면 수동
        if [[ -z "$MANUAL_TOPIC" ]]; then
           echo "🤖 주제 자동 선정 모드"
           python automation/step1_topic_agent.py
        else
           echo "📝 수동 주제 모드: $MANUAL_TOPIC"
           # 파이썬 원라이너로 topic.json 생성
           python3 -c "import json; import os; topic = os.environ['MANUAL_TOPIC']; data = {'title': topic, 'search_keywords': [topic]}; print(json.dumps(data, ensure_ascii=False))" > automation/intermediate_outputs/step1_topic.json
        fi

    # [Step 2] 글 작성 (창작 vs 정리)
    - name: 📝 Step 2 - 글 작성 (Editor Mode)
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        GEMINI_API_KEYS: ${{ secrets.GEMINI_API_KEYS }}
        # 입력된 본문 내용을 환경변수로 전달
        MANUAL_CONTENT: ${{ github.event.inputs.manual_content }}
      run: |
        echo "📝 Step 2 실행..."
        python automation/step2_writer_agent.py

    # [Step 3, 4] 이후 단계는 동일
    - name: 🎨 Step 3 - 이미지 생성 및 검수
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        GEMINI_API_KEYS: ${{ secrets.GEMINI_API_KEYS }}
        HUGGINGFACE_API_TOKEN: ${{ secrets.HUGGINGFACE_API_TOKEN }} 
      run: python automation/step3_image_audit_agent.py
    
    - name: 💾 Step 4 - data.json 저장
      run: python automation/step4_save_to_data_json.py
        
    - name: 🔨 블로그 빌드 (RSS/HTML)
      run: python automation/build_blog.py
    
    - name: 📊 data.json 이동
      run: cp automation/data.json data.json || exit 1
        
    - name: 📋 검수용 PR 생성
      env:
        GH_TOKEN: ${{ secrets.MY_PAT }} 
        TZ: 'Asia/Seoul'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add .
        if ! git diff --staged --quiet; then
          CURRENT_DATE=$(date +'%Y%m%d-%H%M%S')
          BRANCH_NAME="ai-content-$CURRENT_DATE"
          if [ -f automation/intermediate_outputs/step1_topic.json ]; then
            TOPIC=$(cat automation/intermediate_outputs/step1_topic.json | jq -r '.title')
          else
            TOPIC="AI 포스팅"
          fi
          git checkout -b $BRANCH_NAME
          git commit -m "📝 [AI 작성] $TOPIC"
          git push origin $BRANCH_NAME
          gh pr create --title "📝 [AI 작성] $TOPIC" --body "AI가 작성한 새 글입니다." --base main --head $BRANCH_NAME
        fi