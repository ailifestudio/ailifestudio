# 🏗️ AI 블로그 자동화 파이프라인 아키텍처

## 📊 데이터 흐름 (Data Flow)

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Trend & Topic Agent                                 │
│ ─────────────────────────────────────────────────────────── │
│ Script: step1_topic_agent.py                                │
│ Input:  - 현재 날짜                                          │
│         - data.json (기존 제목 리스트)                        │
│ Output: step1_topic.json                                    │
│         {                                                    │
│           "title": "직장인 회의록, AI로 5분 만에 정리",       │
│           "generated_at": "2025-12-14 10:30"                │
│         }                                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Writer & Art Director Agent                         │
│ ─────────────────────────────────────────────────────────── │
│ Script: step2_writer_agent.py                               │
│ Input:  step1_topic.json                                    │
│ Output: step2_structured_content.json                       │
│         {                                                    │
│           "title": "...",                                    │
│           "sections": [                                      │
│             {"type": "heading", "level": 2, ...},           │
│             {"type": "paragraph", ...},                     │
│             {"type": "image_placeholder",                   │
│              "description": "Korean professional..."},      │
│             {"type": "tip_box", ...}                        │
│           ],                                                 │
│           "summary": "...",                                  │
│           "tags": ["AI", "업무자동화"]                       │
│         }                                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Image Generation & Vision Audit Agent              │
│ ─────────────────────────────────────────────────────────── │
│ Script: step3_image_audit_agent.py                          │
│ Input:  step2_structured_content.json                       │
│ Process:                                                     │
│   - 이미지 생성 (Pollinations.ai)                            │
│   - Gemini Vision 검수 (PASS/FAIL)                          │
│ Output: step3_validated_content.json                        │
│         {                                                    │
│           "sections": [                                      │
│             ...                                              │
│             {"type": "image",                               │
│              "url": "automation/generated_images/...",      │
│              "audit_status": "PASS"},                       │
│             {"type": "image_placeholder",                   │
│              "audit_status": "FAIL", "removed": true}       │
│           ]                                                  │
│         }                                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Save to data.json                                   │
│ ─────────────────────────────────────────────────────────── │
│ Script: step4_save_to_data_json.py                          │
│ Input:  step3_validated_content.json                        │
│ Output: data.json (업데이트)                                 │
│         + Markdown 파일 생성 (contents/*.md)                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 5: Blog Build (HTML Rendering)                        │
│ ─────────────────────────────────────────────────────────── │
│ - GitHub Pages: script.js (data.json → HTML)               │
│ - WordPress: API 연동 (향후 구현)                            │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 핵심 설계 원칙

1. **데이터 중심 (Data-Centric)**
   - HTML은 최종 표현 계층에서만 생성
   - 모든 중간 처리는 구조화된 JSON

2. **독립적 실행 가능**
   - 각 Step은 이전 Step의 JSON 출력만 필요
   - 실패 시 해당 Step만 재실행

3. **검증 가능한 파이프라인**
   - 각 Step의 출력 JSON을 검사 가능
   - Gemini Vision의 이미지 품질 검수

4. **확장 가능한 구조**
   - WordPress, Notion, Medium 등 다양한 출력 지원
   - 새로운 콘텐츠 타입 추가 용이

## 📂 파일 구조

```
automation/
├── step1_topic_agent.py
├── step2_writer_agent.py
├── step3_image_audit_agent.py
├── step4_save_to_data_json.py
├── intermediate_outputs/
│   ├── step1_topic.json
│   ├── step2_structured_content.json
│   └── step3_validated_content.json
└── generated_images/
    ├── img_abc123.png
    └── thumbnail_xyz789.png

data.json (최종 데이터)
contents/*.md (Markdown 파일)
```

## 🔧 GitHub Actions 워크플로우

```yaml
- name: ✍️ Step 1 - 주제 선정
  run: python automation/step1_topic_agent.py

- name: 📝 Step 2 - 글 작성 (구조화된 데이터)
  run: python automation/step2_writer_agent.py

- name: 🎨 Step 3 - 이미지 생성 및 검수
  run: python automation/step3_image_audit_agent.py

- name: 💾 Step 4 - data.json 저장
  run: python automation/step4_save_to_data_json.py

- name: 📤 Git 커밋 & 푸시
  run: |
    git add .
    git commit -m "🤖 자동 배포: 블로그 빌드 완료"
    git push
```
